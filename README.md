# Fish Tank VR

Интерактивное веб-приложение для отображения 3D моделей с эффектом "окна в виртуальное пространство". Приложение использует трекинг головы пользователя для создания реалистичного эффекта параллакса, создавая ощущение, что экран является порталом в трехмерный мир.

## 🎯 Основные возможности

- **Head Tracking**: Отслеживание позиции головы через веб-камеру с использованием MediaPipe
- **Off-Axis Projection**: Продвинутая техника проекции для создания эффекта "окна в 3D"
- **Интерактивное управление**: Управление 3D моделью через мышь или кнопки интерфейса
- **Калибровка**: Настройка параметров экрана для оптимального эффекта
- **Реалистичное освещение**: Настроенная система освещения для объемной визуализации моделей

## 🚀 Быстрый старт

### Требования

- Node.js 18+ и npm
- Современный браузер с поддержкой WebGL (Chrome, Firefox, Safari)
- Веб-камера (опционально, можно использовать режим управления мышкой)

### Установка

```bash
# Клонируйте репозиторий (или используйте существующую папку)
cd fish-tank-vr2

# Установите зависимости
npm install

# Запустите dev-сервер
npm run dev
```

Откройте браузер и перейдите по адресу `http://localhost:5173`

### Первый запуск

1. Разрешите доступ к веб-камере при запросе браузера
2. Двигайте головой - вы увидите, как перспектива 3D сцены меняется в реальном времени
3. Используйте панель управления слева для настройки параметров

## 📦 Структура проекта

```
fish-tank-vr2/
├── public/
│   └── models/              # Папка для 3D моделей (.glb, .gltf)
│       ├── pokemon_card.glb # Текущая модель
│       └── ...
├── src/
│   ├── components/          # React компоненты
│   │   ├── ControlPanel.tsx # Панель управления
│   │   ├── FaceMeshView.tsx # Виджет камеры с трекингом
│   │   └── ThreeView.tsx    # Контейнер для Three.js сцены
│   ├── utils/
│   │   ├── threeScene.ts    # Управление 3D сценой и освещением
│   │   ├── offAxisCamera.ts # Математика off-axis проекции
│   │   ├── headPose.ts      # Обработка данных трекинга
│   │   └── calibration.ts   # Калибровка экрана
│   └── App.tsx              # Главный компонент
└── package.json
```

## 🎨 Подключение своей 3D модели

### Шаг 1: Подготовка модели

Поддерживаемые форматы:
- **GLB** (рекомендуется) - бинарный формат, один файл
- **GLTF** - текстовый формат, может требовать дополнительные файлы

Где найти модели:
- [Sketchfab](https://sketchfab.com) - большая коллекция бесплатных моделей
- [Poly Haven](https://polyhaven.com/models) - качественные CC0 модели
- [TurboSquid](https://www.turbosquid.com) - платные и бесплатные модели
- [Free3D](https://free3d.com) - бесплатные модели

### Шаг 2: Размещение файла

Поместите файл модели в папку `public/models/`:

```bash
# Пример: копирование модели
cp /path/to/your/model.glb public/models/my_model.glb
```

### Шаг 3: Изменение пути в коде

Откройте файл `src/utils/threeScene.ts` и найдите метод `loadShoeModel()`:

```typescript
loader.load(
  '/models/pokemon_card.glb',  // ← Измените на путь к вашей модели
  (gltf) => {
    this.model = gltf.scene;
    // ...
  }
);
```

Измените путь на ваш файл:
```typescript
loader.load(
  '/models/my_model.glb',  // Ваш файл
  (gltf) => {
    // ...
  }
);
```

### Шаг 4: Настройка позиции и масштаба

В том же методе настройте начальную позицию, ротацию и масштаб модели:

```typescript
this.model.position.set(0, 0, 0);      // X, Y, Z позиция
this.model.rotation.set(0, 0, 0);      // X, Y, Z ротация (в радианах)
this.model.scale.set(0.071, 0.071, 0.071);  // Масштаб по осям
```

**Советы по настройке:**
- Если модель слишком большая/маленькая, измените значения `scale`
- Для поворота модели используйте `rotation.set(x, y, z)` где значения в радианах
- Для смещения используйте `position.set(x, y, z)`

### Шаг 5: Оптимизация модели (опционально)

Для лучшей производительности:
- Используйте GLB формат (бинарный, быстрее загружается)
- Оптимизируйте количество полигонов (до 50k-100k для веб)
- Сожмите текстуры (рекомендуется 1024x1024 или меньше)

Инструменты для оптимизации:
- [glTF-Pipeline](https://github.com/CesiumGS/gltf-pipeline) - CLI инструмент
- [Blender](https://www.blender.org) - 3D редактор с экспортом в GLB

## 💡 Настройка освещения

Освещение настраивается в файле `src/utils/threeScene.ts` в методе `loadShoeModel()`.

### Типы источников света

#### 1. Ambient Light (Рассеянный свет)
Создает базовую подсветку всей сцены:

```typescript
const ambientLight = new THREE.AmbientLight(0xffffff, 0.3);
// Параметры: цвет, интенсивность (0-1)
this.scene.add(ambientLight);
```

**Рекомендации:**
- Интенсивность: 0.2-0.4 (слишком высокое значение убирает контраст)
- Используется для базовой видимости объектов

#### 2. Directional Light (Направленный свет)
Создает параллельные лучи света (как солнце):

```typescript
const frontLight = new THREE.DirectionalLight(0xffffff, 1.8);
frontLight.position.set(0, 0, 3);  // Позиция источника
// Интенсивность: 0.5-2.0
this.scene.add(frontLight);
```

**Позиционирование:**
- `position.set(x, y, z)` - направление света (не позиция источника)
- `(0, 0, 3)` - свет спереди модели
- `(2, 3, 1)` - свет сверху справа
- `(-2, 1, 1)` - свет слева

#### 3. Point Light (Точечный свет)
Свет, излучающийся из одной точки во все стороны:

```typescript
const pointLight = new THREE.PointLight(0xffffff, 1.5, 10);
pointLight.position.set(0, 0, 2.5);  // Позиция в 3D пространстве
// Параметры: цвет, интенсивность, расстояние затухания
this.scene.add(pointLight);
```

**Рекомендации:**
- Интенсивность: 0.5-2.0
- Расстояние: 5-15 (чем больше, тем дальше распространяется свет)
- Отлично подходит для создания бликов

### Текущая конфигурация освещения

В проекте используется следующая схема:

1. **Ambient Light** (0.3) - базовая подсветка
2. **Front Light** (1.8) - основной свет спереди для бликов
3. **Front Top Light** (1.2) - дополнительный свет сверху спереди
4. **Side Lights** (0.8, 0.5) - боковое освещение для объема
5. **Point Lights** (1.5, 0.8) - точечные источники для деталей

### Настройка материалов для лучших бликов

В коде автоматически настраиваются материалы моделей:

```typescript
// В методе loadShoeModel(), после загрузки модели:
this.model.traverse((child) => {
  if (child instanceof THREE.Mesh) {
    if (child.material instanceof THREE.MeshStandardMaterial) {
      child.material.metalness = 0.2;    // Металличность (0-1)
      child.material.roughness = 0.2;    // Шероховатость (0-1)
      // Чем меньше roughness, тем больше бликов
    }
  }
});
```

**Параметры материалов:**
- `metalness` (0-1): Металлический оттенок. 0 = диэлектрик, 1 = металл
- `roughness` (0-1): Шероховатость поверхности. 0 = зеркало, 1 = матовая поверхность
- Для глянцевых поверхностей: `roughness = 0.1-0.3`
- Для матовых поверхностей: `roughness = 0.5-0.8`

### Примеры конфигураций освещения

#### Яркое фронтальное освещение (для карточек, документов)
```typescript
const ambientLight = new THREE.AmbientLight(0xffffff, 0.2);
const frontLight = new THREE.DirectionalLight(0xffffff, 2.0);
frontLight.position.set(0, 0, 3);
const pointLight = new THREE.PointLight(0xffffff, 1.5, 10);
pointLight.position.set(0, 0, 2.5);
```

#### Мягкое объемное освещение (для органических объектов)
```typescript
const ambientLight = new THREE.AmbientLight(0xffffff, 0.4);
const topLight = new THREE.DirectionalLight(0xffffff, 1.0);
topLight.position.set(0, 3, 2);
const fillLight = new THREE.DirectionalLight(0xffffff, 0.6);
fillLight.position.set(-2, 1, 1);
```

#### Драматическое освещение (для демонстрации)
```typescript
const ambientLight = new THREE.AmbientLight(0xffffff, 0.15);
const keyLight = new THREE.DirectionalLight(0xffffff, 2.5);
keyLight.position.set(2, 3, 2);
const rimLight = new THREE.DirectionalLight(0xffffff, 1.0);
rimLight.position.set(-2, 1, -1);
```

## 🎮 Управление

### Панель управления (слева)

**Заголовок:**
- 🖼️ **Fullscreen** - полноэкранный режим
- 📦 **Debug mode** - показать оси координат и маркер позиции головы

**Tracking:**
- Статус трекинга (OK/Lost)
- Координаты головы (X, Y, Z)
- FPS счетчик

**Parallax Calibration:**
- **Screen Width/Height** - физические размеры экрана в см
- **Viewing Distance** - расстояние от глаз до экрана в см
- **Aspect Ratio** - автоматически вычисляемое соотношение сторон

**Model Controls:**
- **Position X/Y/Z** - слайдеры для позиции модели
- **Scale** - масштаб модели
- **Rotation** - вращение модели
- 🖱️ **Mouse Control** - кнопка для управления моделью мышкой

### Управление мышкой

1. Нажмите кнопку с иконкой мышки в секции "Model Controls"
2. Курсор изменится на `grab`
3. Зажмите левую кнопку мыши и двигайте - модель будет перемещаться
4. Отпустите кнопку мыши для завершения перемещения

## 🔧 Технические детали

### Технологический стек

- **Vite** - быстрая сборка и dev-сервер
- **React 18** - UI фреймворк
- **TypeScript** - типизированный JavaScript
- **Three.js** - 3D рендеринг через WebGL
- **MediaPipe Tasks Vision** - трекинг головы через FaceLandmarker
- **Tailwind CSS** - стилизация интерфейса

### Off-Axis Projection

Приложение использует технику off-axis projection для создания эффекта "окна в 3D":

- Экран рассматривается как физический QUAD в 3D пространстве
- Проекционная матрица строится на основе позиции глаза относительно экрана
- View matrix формируется из базисных векторов экрана
- Это создает реалистичный эффект параллакса при движении головы

### Head Tracking

- Использует MediaPipe FaceLandmarker для детекции лица
- Оценка позиции X/Y из центра лица (нормализованные координаты)
- Оценка Z из межзрачкового расстояния (IPD)
- EMA сглаживание для стабильности движения
- Автоматический возврат к центру при потере лица (>0.5 сек)

## 🐛 Решение проблем

### Камера не работает

- Убедитесь, что браузер имеет доступ к камере (проверьте настройки браузера)
- Используйте режим управления мышкой (кнопка с иконкой мышки)
- Попробуйте другой браузер (Chrome рекомендуется)

### Модель не отображается

- Проверьте путь к файлу в `threeScene.ts`
- Убедитесь, что файл находится в `public/models/`
- Проверьте консоль браузера на наличие ошибок загрузки
- Убедитесь, что формат файла поддерживается (GLB/GLTF)

### Низкая производительность

- Уменьшите разрешение камеры в настройках браузера
- Оптимизируйте 3D модель (меньше полигонов)
- Отключите debug mode
- Закройте другие вкладки браузера

### Эффект параллакса не работает

- Проверьте калибровку размеров экрана
- Настройте расстояние до экрана
- Убедитесь, что трекинг активен (статус "OK" в панели)
- Попробуйте изменить параметры калибровки

## 📝 Лицензия

MIT

## 🙏 Благодарности

- Three.js - мощная библиотека для 3D графики
- MediaPipe - технологии машинного обучения от Google
- Johnny Chung Lee - вдохновение для fish-tank VR эффекта
